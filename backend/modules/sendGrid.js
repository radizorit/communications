
module.exports.sendGrid = function sendGrid(response) {
    //clean this up
    //sendgrid function here
    console.log(`${response.destination} is sent a message! The message 
                "${response.message}" through SendGrid, thanks to the helpful guys at SendGrid`)
}